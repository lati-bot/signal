# Database
DATABASE_URL=postgresql+asyncpg://signal:signal@localhost:5432/signal

# Kalshi (requires API key for authenticated endpoints)
KALSHI_API_KEY=
KALSHI_API_SECRET=

# Manifold (optional, increases rate limits)
MANIFOLD_API_KEY=

# Polymarket (public API, no key needed for reads)
